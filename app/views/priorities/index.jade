extends ../shared/template
block content

  h2 Priority List

  table
    thead
      th Name
      th Color
      th Value
    tbody

    each priority in priorities
      tr
        td= priority.name
        td(style='background-color: #{priority.color};')= priority.color
        td= priority.value
